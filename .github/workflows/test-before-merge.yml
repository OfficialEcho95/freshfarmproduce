name: Run Tests Before Merge

on:
  pull_request:
    branches:
      - main  # or your default branch

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Set up Docker Compose
      uses: ndeloof/setup-docker-compose@v1

    - name: Run Docker Compose
      run: docker-compose -f docker-compose.test.yml up --build -d

    - name: Wait for services
      run: sleep 10  # adjust if needed based on your containers

    - name: Run tests
      run: docker-compose -f docker-compose.test.yml exec -T <test-service> npm test  # or your test command

    - name: Tear down
      run: docker-compose -f docker-compose.test.yml down
